export enum SpawnScreen {
  STAKING_CHECK = 'staking-check',
  RPC = 'rpc',
  AGENT_FUNDING = 'agent-funding',
  DONE = 'done',
  MASTER_WALLET_FUNDING = 'master-wallet-funding',
  ERROR = 'error',
}
